Currently, the drivers are hardcoded in DataManager.cs file. We want the manager to add/remove drivers as well to the system. It means the drivers need to be read from a file and saved to a file as well (just like stops).

This feature will require new menu items to the Manager Dashboard to add remove/drivers.